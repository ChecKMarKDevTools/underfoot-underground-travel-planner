#############################################
# Underfoot Frontend Environment Example
# Copy to .env (do NOT commit secrets). Vite only exposes vars prefixed with VITE_.
#############################################

# Existing sample backend API base (unused by n8n chat, kept for future integration)
VITE_API_BASE=http://localhost:3000
VITE_LIMIT=5

# -------------------------------------------
# n8n Chat Integration
# Choose ONE primary mode:
#   1. Hosted iframe (simplest) via VITE_N8N_CHAT_IFRAME_URL
#   2. Embedded widget (dynamic @n8n/chat) via VITE_N8N_CHAT_WEBHOOK_URL
# If both are set, iframe wins (explicit, predictable precedence).
# -------------------------------------------

# Option 1: Hosted n8n chat UI iframe URL
# Example: https://n8n.example.com/chat/bot
VITE_N8N_CHAT_IFRAME_URL=

# Option 2: n8n Chat Trigger webhook URL (public) for embedded widget
# Example: https://n8n.example.com/webhook/abc123-chat
VITE_N8N_CHAT_WEBHOOK_URL=

# Optional JSON metadata passed in widget mode only.
# MUST be valid JSON (single line). Will be ignored in iframe mode.
# Example: {"appVersion":"1.0.0","channel":"web"}
VITE_N8N_CHAT_METADATA={"appVersion":"dev"}

# Force iframe mode locally for smoke tests (no env override needed):
#   http://localhost:5173/n8n-chat?iframe=1
