// Placeholder test after removal of N8nChatPage (embedded/chat widget path).
// We removed support for the external n8n chat embed and associated widget.
// This stub keeps the test suite structure stable until CI expectations updated.
import { describe, it, expect } from 'vitest';

describe('Removed N8nChatPage', () => {
  it('is deprecated and replaced by native Chat UI', () => {
    expect(true).toBe(true);
  });
});
// (Removed) Legacy N8nChatPage tests intentionally cleared during cleanup of embedded chat path.
